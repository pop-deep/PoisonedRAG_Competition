{
  "model_info": {
    "provider": "openai",
    "name": "gpt-3.5-turbo"
  },
  "api_key_info": {
    "api_keys": [
      "YOUR_API_KEY_HERE"
    ],
    "api_key_use": 0
  },
  "base_url": null,
  "params": {
    "temperature": 0.0,
    "max_output_tokens": 150,
    "seed": 100
  },
  "_comment": "Set base_url to point to any OpenAI-compatible endpoint (OpenAI, DeepSeek, local vLLM, Ollama, etc.). Leave api_keys as placeholder and set env OPENAI_API_KEY / OPENAI_BASE_URL / OPENAI_MODEL instead. If no key is found, the system falls back to MockLLM (offline)."
}
